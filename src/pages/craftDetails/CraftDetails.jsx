import { useLoaderData } from "react-router-dom";

const CraftDetails = () => {
  const data = useLoaderData();
  console.log(data);
  return (
    <div>
      
    </div>
  );
};

export default CraftDetails;